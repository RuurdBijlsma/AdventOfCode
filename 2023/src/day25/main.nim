import std/strutils
import std/strformat
import std/tables
import std/sequtils
import std/math
import std/sugar
import std/algorithm

proc part1*(): int =
  const input = staticRead("input")
  const lines = input.splitLines()
  
  return 0

proc part2*(): int =
  const input = staticRead("input")
  const lines = input.splitLines()
  
  return 0
